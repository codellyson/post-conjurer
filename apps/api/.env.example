# Empty API_TOKEN trusts localhost (dev). Set one to require a bearer token;
# the desktop app stores the matching value in the OS keychain under "bearer".
API_TOKEN=
PORT=8787
# DATA_DIR overrides where the SQLite file lives (defaults to ~/.post-conjurer).
# DATA_DIR=
